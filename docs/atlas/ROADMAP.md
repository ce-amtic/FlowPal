# Roadmap

> Short-term goals. For long-term project goals see PROJECT.md.
>
> This file holds only the current milestone. Open questions and committed
> designs are records under `records/`; finished work is in `git log`.
> Resist adding "Up next" / "Backlog" / "Recently done" sections — they
> duplicate state that already has a single source of truth elsewhere and rot.

## Current milestone

信息采集管道可演示：文本与图片投入 → LLM 抽取（JSON 输出、相对日期解析）→ 条目列表可查看可编辑，数据落本地存储。对应设计文档 §7 的第一批加图片入口。

退出标准：设计文档 §8 的演示碎片集（群聊截图、含截止日的邮件、口头风格文字、改期通知、几条念头、一条进度）能走完管道的每个分支，结果在条目列表里可见、可改。
