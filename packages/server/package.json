{
  "name": "@flowpal/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check:fixtures": "tsx scripts/check-fixtures.ts",
    "check:sync": "tsx scripts/check-sync.ts",
    "sync:once": "tsx scripts/sync-once.ts",
    "bench:now": "tsx scripts/bench-now.ts",
    "demo:reset": "tsx scripts/demo-reset.ts"
  },
  "//": "这个包不依赖 electron，也不依赖 @flowpal/app。将来要把它抽成独立后端，是把 packages/server 与 packages/shared 拷进新仓库、一行不改。scripts/check-fixtures.ts 里有一条断言扫这里对 electron 的引用。",
  "dependencies": {
    "@flowpal/shared": "workspace:*",
    "@hono/node-server": "^1.13.7",
    "hono": "^4.6.14",
    "imapflow": "^1.7.8",
    "mailparser": "^3.9.20",
    "openai": "^4.77.0",
    "tough-cookie": "^5.1.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/mailparser": "^3.4.6",
    "@types/node": "^24.0.0"
  }
}
