import { Empty } from '../../shell/State.tsx'

/**
 * 项目——每个项目一张卡：一句状态、未完成、已完成、用户说过的话，
 * 以及「多久没动」。顶部是「未归类」组。
 *
 * 「多久没动」是这一页的核心：它测的是回避而非进展，而回避正是这道题的靶心。
 * 这一页不放进度条——进度追踪结构性不可解，摆一个百分比比没有更糟。
 *
 * 项目接口尚未落地，这一页目前只呈现空态。
 */
export function ProjectsPage() {
  return (
    <>
      <h1 className="page-title">项目</h1>
      <Empty>还没有项目。同步课表之后，每门课会成为一个。</Empty>
    </>
  )
}
