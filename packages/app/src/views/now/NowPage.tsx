import { Empty } from '../../shell/State.tsx'

/**
 * 此刻——第一眼不是列表，是一件事。
 *
 * 「不知道先干哪个」正是用户卡住的原因，给他一张表等于把问题原样退回。
 * 主推那件、它的第一步、一句依据、以及若干备选由语义层一次返回；
 * 「换一件」与「更小的一步」是本地切换，点击零延迟。
 *
 * 挑选接口尚未落地，这一页目前只呈现空态。
 */
export function NowPage() {
  return (
    <>
      <h1 className="page-title">此刻</h1>
      <Empty>还没有可以开始的事。扔点什么进来，或者从日程里挑一件。</Empty>
    </>
  )
}
