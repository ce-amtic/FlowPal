/**
 * 图标的尺寸与描边。两个数，全 App 只在这里写一次。
 *
 * 描边粗细比尺寸更要紧：同一屏里混着 1.5 和 2 的图标，看起来像两套图标库，
 * 而这种不齐很难被指出来，只会让人觉得「有点乱」。
 */
export const ICON = {
  /** 与正文同高，跟文字并排时不抢 */
  size: 16,
  /** 角标、气泡里更小的一档 */
  sizeSmall: 14,
  stroke: 1.75,
} as const
