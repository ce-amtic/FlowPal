node_modules/
dist/
*.tsbuildinfo

# 本地凭据：LLM key 与教务系统学号密码，永不进仓库
config.local.json

# 用户数据：SQLite 与落盘的图片 / 文件
data/*.db
data/*.db-*
data/blobs/
# check:fixtures 每次跑都重建的一次性库
data/check-run/

.DS_Store
