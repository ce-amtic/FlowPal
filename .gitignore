node_modules/
dist/
*.tsbuildinfo

# 本地凭据：LLM key 与学校邮箱的客户端授权码，永不进仓库
config.local.json

# 工具在仓库里开的临时工作树。里面有一份完整的仓库副本，config.local.json 也在其中
.claude/worktrees/

# 用户数据：SQLite 与落盘的图片 / 文件
data/*.db
data/*.db-*
data/blobs/
# 学校门户的登录态。它等价于一次登录，进仓库就是把账号交出去
data/ruc-cookies.json
# check:fixtures 每次跑都重建的一次性库
data/check-run/

.DS_Store
